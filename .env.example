# CDW login credentials
CDW_EMAIL=you@yourcompany.com
CDW_PASSWORD=your_cdw_password

# From your Slack app settings → Basic Information → App Credentials
SLACK_SIGNING_SECRET=your_slack_signing_secret

# Set automatically by Railway — do not change
PORT=3000
